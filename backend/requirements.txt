# Core Flask dependencies
flask==3.0.0
flask-cors==4.0.0
werkzeug==3.0.1

# Image processing
# Using versions with good wheel availability for cloud platforms
pillow>=10.0.0,<11.0.0
numpy>=1.24.0,<1.27.0

# OpenCV - Headless version for server environments
# Using version range to allow pip to find compatible wheel
# Headless doesn't require GUI libraries (better for cloud deployment)
opencv-python-headless>=4.8.0,<5.0.0

# OCR - Optical Character Recognition (optional)
# Note: Tesseract OCR binary must be installed on the system
# On Render: Tesseract may not be available, OCR will be disabled gracefully
# The app works without OCR - it will use filename-based detection
pytesseract>=0.3.10

# Environment variables
python-dotenv>=1.0.0

# Note: Removed unused packages (scikit-learn, scikit-image, sqlalchemy)
# to reduce build time and avoid dependency conflicts on Render

